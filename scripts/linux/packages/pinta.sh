#!/bin/bash
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
#
# install pinta image editor
# add pppa + install pinta package
#
# (!) it is currently not possible to install via commandline, becuase "repo does not have a Release file"
#     bug is already reported, but not fixed. s. =>
#     https://bugs.launchpad.net/pinta/+bug/1607586
#
# as long is not fixed, we have to install via software center
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
# sudo add-apt-repository ppa:pinta-maintainers/pinta-stable -y && sudo apt update && sudo apt install
